import type { Metadata } from "next";
import "./globals.css";
import { WebSocketProvider } from "@/contexts/WebSocketContext";

export const metadata: Metadata = {
  title: "QuailBreeder",
  description: "Accounting for quail breeding - Take control of your business with poultry management software",
  icons: {
    icon: '/hatching-quail-icon.webp',
    apple: '/hatching-quail-icon.webp',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased min-h-screen bg-gray-50 dark:bg-gray-900">
        <WebSocketProvider>{children}</WebSocketProvider>
      </body>
    </html>
  );
}
