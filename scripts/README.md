# Автоматическая генерация маршрутов из словарей

## Проблема

Ранее маршруты приходилось добавлять вручную в три места:
1. В словари (`config/dictionaries/*.json`) - секция `"routes"`
2. В `config/routes.ts` - маппинги `routeMapping` и `reverseRouteMapping`
3. В `next.config.js` - массив `rewrites()`

Это приводило к дублированию кода и возможности ошибок.

## Решение

Теперь маршруты автоматически генерируются из словарей!

### Как это работает

1. **Источник истины** - секция `"routes"` в каждом словаре (`config/dictionaries/*.json`):
   ```json
   {
     "routes": {
       "about": "about",
       "getStarted": "get-started",
       "privacyPolicy": "privacy-policy",
       "termsOfUse": "terms-of-use"
     }
   }
   ```

2. **Скрипт генерации** (`scripts/generate-routes.js`):
   - Читает все словари из `config/dictionaries/`
   - Извлекает секцию `"routes"` из каждого
   - Генерирует два файла:
     - `config/routes.generated.ts` - TypeScript маппинги для использования в коде
     - `config/rewrites.generated.js` - rewrites для Next.js

3. **Интеграция**:
   - `config/routes.ts` реэкспортирует функции из `routes.generated.ts`
   - `next.config.js` использует `rewrites.generated.js` в функции `rewrites()`

## Использование

### Добавление нового маршрута

1. Добавьте маршрут в секцию `"routes"` **всех** словарей:

   **config/dictionaries/en.json:**
   ```json
   {
     "routes": {
       "about": "about",
       "getStarted": "get-started",
       "newPage": "new-page"  // ← Новый маршрут
     }
   }
   ```

   **config/dictionaries/de.json:**
   ```json
   {
     "routes": {
       "about": "ueber-uns",
       "getStarted": "erste-schritte",
       "newPage": "neue-seite"  // ← Переведенный slug
     }
   }
   ```

2. Запустите генерацию:
   ```bash
   npm run generate:routes
   ```

3. Готово! Маршруты автоматически обновятся в `config/routes.generated.ts` и `config/rewrites.generated.js`

### Важные правила

1. **Английский - эталон**: Английские slugs должны совпадать с физическими папками в `app/[lang]/`
2. **Ключи одинаковые**: Все языки должны иметь одинаковые ключи в секции `"routes"`
3. **camelCase для ключей**: Используйте camelCase для ключей (`getStarted`, не `get-started`)
4. **kebab-case для значений**: Используйте kebab-case для URL slugs (`get-started`, не `getStarted`)

## Преимущества

✅ **Единственный источник истины** - маршруты определяются только в словарях
✅ **Автоматическая синхронизация** - изменения применяются ко всей системе
✅ **Меньше ошибок** - невозможно забыть обновить один из файлов
✅ **Легко добавлять новые языки** - просто добавьте новый словарь и запустите скрипт

## Файлы

- `scripts/generate-routes.js` - скрипт генерации
- `config/routes.generated.ts` - 🤖 сгенерированные TypeScript маппинги
- `config/rewrites.generated.js` - 🤖 сгенерированные Next.js rewrites
- `config/routes.ts` - реэкспортирует функции из generated файла
- `next.config.js` - использует сгенерированные rewrites

**⚠️ Не редактируйте файлы с `.generated.` в названии вручную!**
