# Quick Start Guide

## 🚀 Недавно добавленные функции

### 1. Исправлена форма поддержки
Форма `SupportTicketForm` теперь корректно отправляет данные на сервер.

### 2. Email-уведомления о новых пользователях
Каждая новая регистрация через Google/Apple отправляет email администратору.

### 3. Автоматическая регенерация Apple JWT
**Важно:** JWT для Apple OAuth теперь регенерируется автоматически!

### 4. IMAP Sent Folder
**Новое:** Все отправленные email теперь сохраняются в папку "Отправленные"!

#### Проверка JWT вручную
```bash
node scripts/check-apple-jwt.js
```

#### Вывод
- ✅ Зелёный - JWT валиден, всё хорошо
- ⚠️  Жёлтый - JWT регенерирован, `.env` обновлён
- ❌ Красный - Ошибка, требуется внимание

#### Что происходит автоматически
- При каждом запуске `ci.sh` проверяется JWT
- Если до истечения < 30 дней, генерируется новый
- Email-уведомление отправляется администратору
- Docker-образ собирается с новым JWT

## 📧 Email-уведомления

Все email отправляются на: `quailbreeding@gmail.com`

**Типы уведомлений:**
1. 🎉 Новый пользователь зарегистрировался
2. 🔐 Apple JWT регенерирован
3. 🎫 Новый support ticket

**📬 Важно:** Все письма автоматически сохраняются в IMAP папку "Отправленные"!

## 🔧 Быстрая отладка

### Форма не отправляется?
```bash
# Проверьте логи браузера
# Убедитесь, что поля имеют атрибут name
```

### Email не приходят?
```bash
# Проверьте SMTP настройки в .env
cat .env | grep SMTP

# Тестовая отправка
node scripts/send-jwt-notification.js "2025-01-15T00:00:00Z" "2025-07-15T00:00:00Z"
```

### JWT проблемы?
```bash
# Ручная проверка
node scripts/check-apple-jwt.js

# Принудительная генерация
node scripts/generate-apple-secret.js
```

## 📚 Полная документация

- Apple JWT: `scripts/README-JWT.md`
- IMAP Sent Folder: `docs/IMAP-SENT-FOLDER.md`
- Полная сводка: `IMPLEMENTATION_SUMMARY.md`

## 🧪 Тесты

```bash
# Проверка Apple JWT
node scripts/check-apple-jwt.js

# Проверка IMAP (только в Docker)
node scripts/test-imap.js
```

---

**Последнее обновление:** 2025-12-30
