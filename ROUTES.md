# 🚀 Автоматизация SEO (маршруты, sitemap, robots.txt)

## Быстрый старт

### Добавление нового переведенного маршрута

1. **Откройте словари** и добавьте маршрут в секцию `"routes"` во ВСЕХ языках:

   ```json
   // config/dictionaries/en.json
   {
     "routes": {
       "about": "about",
       "getStarted": "get-started",
       "yourNewPage": "your-new-page"  // ← Добавьте здесь
     }
   }
   ```

   ```json
   // config/dictionaries/de.json
   {
     "routes": {
       "about": "ueber-uns",
       "getStarted": "erste-schritte",
       "yourNewPage": "ihre-neue-seite"  // ← И здесь (переведенный slug)
     }
   }
   ```

2. **Запустите генерацию**:
   ```bash
   npm run generate:seo
   ```

   Или по отдельности:
   ```bash
   npm run generate:routes  # Только маршруты и rewrites
   npm run generate:robots  # Только robots.txt
   ```

3. **Готово!** Автоматически обновятся:
   - Маршруты (config/routes.generated.ts)
   - Rewrites (config/rewrites.generated.js)
   - robots.txt (public/robots.txt)
   - sitemap.xml (при следующем запуске сервера)

## Что происходит под капотом

Скрипт `scripts/generate-routes.js`:
- ✅ Читает секцию `"routes"` из всех словарей
- ✅ Генерирует `config/routes.generated.ts` (TypeScript маппинги)
- ✅ Генерирует `config/rewrites.generated.js` (Next.js rewrites)
- ✅ Автоматически интегрируется с `config/routes.ts` и `next.config.js`

## Правила

- **Ключи** - используйте `camelCase` (например: `getStarted`, `privacyPolicy`)
- **Значения** - используйте `kebab-case` (например: `get-started`, `privacy-policy`)
- **Английский** - значения должны совпадать с физическими папками в `app/[lang]/`
- **Все языки** - должны иметь одинаковые ключи

## Подробности

Смотрите [scripts/README.md](scripts/README.md) для подробной документации.
